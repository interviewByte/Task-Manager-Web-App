{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
{
	"version": 2,
	"rewrites": [{ "source": "/(.*)", "destination": "/server.js" }]
}